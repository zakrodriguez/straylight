# YubiHSM PKCS#11 configuration (Docker container)
# Reaches connector on the Docker host via host.docker.internal
# Managed by Ansible — do not edit manually
connector = http://host.docker.internal:12345
